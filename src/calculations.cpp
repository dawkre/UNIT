#include "calculations.h"

position get_position(/*bbox left_cam_detecion, bbox right_cam_detection*/) {
	position pos = {1.1, 1.2, 1.3};
	return pos;
}

PTZF calculate_PTZF(/*position object_coordinates*/) {
	PTZF ptzf = {50, 50, 50, 50};
	return ptzf;
};